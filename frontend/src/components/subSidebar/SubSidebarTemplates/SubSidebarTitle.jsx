import React from 'react';
import { NavLink } from 'react-router-dom';

const SubSidebarTitle = ({title}) => (
    <div className="subsidebartitle-container">
        <h2 className="subsidebar-maintitle">{title}</h2>
    </div>
);

export default SubSidebarTitle;
